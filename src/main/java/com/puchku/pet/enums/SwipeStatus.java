package com.puchku.pet.enums;

public enum SwipeStatus {
    ACCEPTED("ACCEPTED"),
    PENDING("PENDING"),
    UNMATCHED("UNMATCHED");

    SwipeStatus(String status) {
        // nothing to do
    }
}
